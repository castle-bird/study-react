import styled from "styled-components";

const MainContainer = styled.div``;

const Home = () => {
    return (
        <MainContainer>
            <h1>메인</h1>
        </MainContainer>
    );
};

export default Home;
